<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
                        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

                        
<mapper namespace="com.ydu.biz.cbt.impl.CBTMapper"> 

	<select id="getBoard" resultType="cbtList">
        <![CDATA[
        SELECT * 
        FROM BOARD
        WHERE SEQ = #{seq}
        ]]>
    </select>
	
	<!-- 시험 리스트 조회 : 교수 : 모두 조회 -->
	<select id="getAllCBT" resultType="" parameterType="">
		SELECT *
		FROM CBT_LIST
		ORDER BY CBT_CODE
	</select>
	
	<!-- 시험 리스트 조회 : 학생 : 내가 수강한 것만 조회 -->
	<select id="getMyCBT" resultType="map" >
		SELECT cbt_code as "cbtCode", class_code as "classCode", professor_code as "professorCode",
				cbt_title as "cbtTitle", final_date as "finalDate"
		FROM CBT_LIST
		WHERE classCode in (
			SELECT class_code
			FROM CLASS_STATUS
			WHERE student_code = '2017000101'
		)
		
	</select>
	
</mapper>
