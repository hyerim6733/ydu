<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
                        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ydu.biz.board.impl.BoardMapper">

	<insert id="insertBoard" parameterType="board">
		<selectKey keyProperty="boardId" resultType="int" order="BEFORE">
			SELECT NVL(MAX(BOARD_ID), 0) + 1 FROM BOARD
		</selectKey>
        <![CDATA[
        INSERT INTO BOARD(BOARD_ID, BOARD_CODE, TITLE, WRITE_DATE,EDIT_DATE,CATEGORY, WRITER, CONTENT) 
        VALUES(#{boardId},#{boardCode},#{writeDate},#{editDate},#{category},
        #{title}, #{writer}, #{content})
        ]]>
	</insert>

	<update id="updateBoard">
        <![CDATA[
        UPDATE BOARD SET
            TITLE = #{title}, 
            CONTENT = #{content} 
        WHERE BOARD_ID = #{boardId}
        ]]>
	</update>

	<delete id="deleteBoard" parameterType="board">
        <![CDATA[
        DELETE BOARD
         WHERE BOARD_ID = #{boardId}
        ]]>
	</delete>

	<select id="getBoard" resultType="board">
        <![CDATA[
        SELECT * 
        FROM BOARD
          WHERE BOARD_ID = #{boardId}
        ]]>
	</select>

	<select id="getBoardList" resultType="board"
		parameterType="com.ydu.biz.board.BoardSearchVO">
		SELECT *
		FROM BOARD
		WHERE 1 = 1
		<if test="searchCondition == 'TITLE'">
			AND TITLE LIKE '%'||#{searchKeyword}||'%'
		</if>
		<if test="searchCondition == 'CONTENT'">
			AND CONTENT LIKE '%'||#{searchKeyword}||'%'
		</if>
		ORDER BY BOARD_ID DESC
	</select>

	<delete id="deleteBoardList" parameterType="com.ydu.biz.board.BoardSearchVO">
		DELETE FROM BOARD
		WHERE BOARD_ID IN
		<foreach collection="seqList" item="item" open="(" close=")"
			separator=",">
			#{item}
		</foreach>
	</delete>


</mapper>                    